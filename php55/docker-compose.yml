version: "2"
services:
    php:
        build: .
        image: gcr.io/ste-optical-vts/php55
        environment:
            - MYSQL_ROOT_PASSWORD=secret
        volumes:
            - /var/www/html:/var/www/html
        tmpfs:
            - /var/log
            - /tmp
        # networks:
            # front-tier:
                # aliases:
                    # - php
            # back-tier:
