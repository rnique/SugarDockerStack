version: "2"
services:
    apache:
        build: .
        image: gcr.io/ste-optical-vts/apache24
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - /var/www/html:/var/www/html
        tmpfs:
            - /var/log
            - /tmp
        # networks:
            # - front-tier
